services:
  mongo-test:
    image: mongo:7
    container_name: devtinder-mongo-test
    ports:
      - '27017:27017'
    volumes:
      - mongo-test-data:/data/db

volumes:
  mongo-test-data:
